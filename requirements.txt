# API server
fastapi==0.115.4
uvicorn[standard]==0.32.1

# Request/response model validation
pydantic==2.10.3

# Hugging Face model loading and fine-tuning
transformers==4.48.0
peft==0.13.2
accelerate==1.2.1
datasets==3.2.0

# PyTorch (CPU build; swap for a GPU wheel if needed)
torch==2.6.0

# OpenAI API – synthetic data generation and orchestration
openai==1.57.0

# Prometheus metrics exposed at /metrics
prometheus-client==0.21.0

# Evaluation metrics (accuracy, precision, recall, F1)
scikit-learn==1.6.0

# HTTP client used by the orchestrator to call the verifier
requests==2.32.3
